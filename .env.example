# Flink SQL Gateway Configuration
# Copy this file to .env and configure your settings
# 
# IMPORTANT: Vite only exposes environment variables that start with VITE_ to the browser
# If you have OS environment variables like FLINK_HOST, you need to either:
# 1. Set them with VITE_ prefix (recommended for development)
# 2. Or create this .env file mapping your OS variables

# Option 1: Use VITE_ prefixed variables directly
VITE_FLINK_HOST=http://localhost:8083
VITE_FLINK_USERNAME=your-username
VITE_FLINK_PASSWORD=your-password
VITE_FLINK_API_TOKEN=your-api-token

# Option 2: Map your OS environment variables (if you have FLINK_HOST, FLINK_USERNAME, etc. set in OS)
# VITE_FLINK_HOST=${FLINK_HOST}
# VITE_FLINK_USERNAME=${FLINK_USERNAME} 
# VITE_FLINK_PASSWORD=${FLINK_PASSWORD}
# VITE_FLINK_API_TOKEN=${FLINK_API_TOKEN}

# SSL Certificate Verification (default: true)
# Set to 'false' to disable SSL verification for self-signed certificates
VITE_FLINK_SSL_VERIFY=true

# Examples for different environments:

# Local development with no auth:
# VITE_FLINK_HOST=http://localhost:8083

# Development with basic auth:
# VITE_FLINK_HOST=https://your-flink-server.com:8083
# VITE_FLINK_USERNAME=admin
# VITE_FLINK_PASSWORD=your-password

# Production with token auth:
# VITE_FLINK_HOST=https://your-flink-server.com:8083
# VITE_FLINK_API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Development with self-signed certificates:
# VITE_FLINK_HOST=https://localhost:8083
# VITE_FLINK_SSL_VERIFY=false
